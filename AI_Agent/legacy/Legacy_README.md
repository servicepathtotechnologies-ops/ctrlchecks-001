# Legacy Code
This folder contains legacy Streamlit applications that have been superseded by the `multimodal_backend` API.
- `Text_processing/`: Old text processing UI.
- `image_processing_legacy/`: Old image processing UI.

The active development should happen in `../multimodal_backend`.
